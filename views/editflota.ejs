<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <!--Usamos boxicons -->
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <title>CRUD</title>
  </head>
  <body>
    <% if (coche) { %>
        <div class="container mt-4">
          <div class="card border-secondary" >
              <div class="card-header">Editar</div>
              <div class="card-body" >
                  <form action="/updateflota" method="POST">
      
                      <input id="id" name="id" value="<%= coche.id %>" hidden>
      
                      <div class="mb-3">
                          
      
                        <label for="numero_vehiculo" class="form-label">Numero de coche</label>
                        <input type="text" class="form-control" id="numero_vehiculo" name="numero_vehiculo" value=" <%= coche.numero_vehiculo  %> " tadindex="1">
      
                      </div>
                      <div class="mb-3">
                          <label for="tipo_vehiculo" class="form-label">Tipo de coche</label>
                        <select name="tipo_vehiculo" id="tipo_vehiculo" class="form-select" tabindex="2">
                            <option  <%= coche.tipo_vehiculo=='Camion Compactador' ? "select" : '' %> value="Camion Compactador" selected>Camion compactador</option>
                            <option <%= coche.tipo_vehiculo=='Camion de Volteo' ? "select" : '' %> value="Camion de Volteo">Camion de volteo</option>
                            <option  <%= coche.tipo_vehiculo=='Vehiculo Ligero Furgoneta' ? "select" : '' %> value="Vehiculo Ligero Furgoneta">Vehiculo ligero furgoneta</option>
                            <option <%= coche.tipo_vehiculo=='Camion carga lateral' ? "select" : '' %> value="Camion carga lateral">Camion de carga lateral</option>
                            <option  <%= coche.tipo_vehiculo=='Barredora' ? "select" : '' %> value="Barredora">Barredora</option>
                            
      
                            <label for="capacidad_carga" class="form-label">Capacidad de carga</label>
                        <input type="text" class="form-control" id="capacidad_carga" name="capacidad_carga" value=" <%= coche.capacidad_carga  %> " tadindex="3">
      
                        <label for="estado_vehiculo" class="form-label">Estado del Coche</label>
                        <select name="estado_vehiculo" id="estado_vehiculo" class="form-select" tabindex="4">
                            <option <%= coche.estado_vehiculo=='Disponible' ? "select" : '' %> value="Disponible">Disponible</option>
                            <option <%= coche.estado_vehiculo=='En Reparacion' ? "select" : '' %> value="En Reparacion">En reparacion</option>
                            <option <%= coche.estado_vehiculo=='En Uso' ? "select" : '' %> value="En Uso">En uso</option>
                        </select>
                        </select>
                      </div>
      
                      <div class="mb-3">
                        <label for="fecha_adquisicion" class="form-label">Fecha de adquisición</label>
                        <input type="date" class="form-control" id="fecha_adquisicion" name="fecha_adquisicion"  value=" <%= coche.fecha_adquisicion  %> " tadindex="5">
      
                        <label for="observaciones" class="form-label">Observaciones</label>
                        <input type="text" class="form-control" id="observaciones" name="observaciones"  value=" <%= coche.observaciones  %> " tadindex="6">
      
                      </div>
                      <button type="submit" class="btn btn-primary" tabindex="5">Guardar</button>
                      <a href="/adminflota" class="btn btn-secondary">Cancelar</a>
                      <br>
                          <label for="estatus" class="form-label">Estatus</label>
                          <input type="tinyint" class="form-control" id="estatus" name="estatus" value="<%= coche.estatus %>" tadindex="7">
                  </form>
              </div>
          </div>
        </div>
        <% } %>

      

     <h1> <i class='bx bx-circle-three-quarter' ></i> </h1>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </body>
</html>



